import 'package:flutter/material.dart';

class NewWorkout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Center(
        child: Text('Create your new workout here!'),
      ),
    );
  }
}